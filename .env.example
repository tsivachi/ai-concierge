# Optional: activates AnthropicLLMProvider instead of the default StubLLMProvider.
# The app is fully functional with this unset (research.md §4).
ANTHROPIC_API_KEY=

# Defaults shown; override only if you need a different local DB location.
DATABASE_URL=sqlite:///./concierge.db
CHROMA_PERSIST_DIR=./chroma_data
